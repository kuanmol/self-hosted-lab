replicaCount: 1

image:
  repository: anmolk992/user-service
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8081

env:
  spring:
    datasource:
      url: jdbc:mysql://mysql:3306/shop
      username: app
      password: app

db:
  host: mysql
  name: shop

resources: {}