replicaCount: 1

image:
  repository: order-service
  tag: latest
  pullPolicy: Never

service:
  type: ClusterIP
  port: 8082

env:
  spring:
    datasource:
      url: jdbc:mysql://mysql:3306/shop
      username: app
      password: app

db:
  host: mysql

resources: {}